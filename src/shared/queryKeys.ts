const TRACE_FLAGS = "trace-flags",
  TRACED_ENTITIES = "traced-entities",
  DEBUG_LEVELS = "debug-levels",
  LIST_LOGS = "list-logs",
  GET_LOG_BY_ID = "get-log-by-id";

const queryKeys = {
  TRACE_FLAGS,
  TRACED_ENTITIES,
  DEBUG_LEVELS,
  LIST_LOGS,
  GET_LOG_BY_ID,
};

export default queryKeys;
