const TRACE_FLAGS = "trace-flags",
  TRACED_ENTITIES = "traced-entities",
  DEBUG_LEVELS = "debug-levels",
  LIST_LOGS = "list-logs",
  GET_LOG = "get-log",
  LIST_LIMITS = "list-limits",
  LIST_ORGS = "list-orgs",
  CURRENT_USER = "current-user";

const queryKeys = {
  TRACE_FLAGS,
  TRACED_ENTITIES,
  DEBUG_LEVELS,
  LIST_LOGS,
  GET_LOG,
  LIST_LIMITS,
  LIST_ORGS,
  CURRENT_USER,
};

export default queryKeys;
